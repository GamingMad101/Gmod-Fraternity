--[[local teleporter = ents.Create("GMF_Teleporter")
	if ( !IsValid( teleporter ) ) then return end // Check whether we successfully made an entity, if not - bail
	teleporter:SetPos(Vector(0,0,64))
	teleporter:Spawn()]]-- (960,1134)  combine
